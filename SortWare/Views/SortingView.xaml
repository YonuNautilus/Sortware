<UserControl x:Class="SortWare.SortingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SortWare"
             mc:Ignorable="d" >
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Resources/CommonWPFResources.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>
  <Grid Height="840">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="1000"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="300"/>
      <ColumnDefinition Width="600"/>
      <ColumnDefinition Width="300"/>
    </Grid.ColumnDefinitions>

    <StackPanel x:Name="RootSortDirAndControlPanel" Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
      <Grid x:Name="DirSelctionGrid" Width="Auto">
        <Grid.RowDefinitions>
          <RowDefinition Height="30"/>
          <RowDefinition Height="30"/>
          <RowDefinition Height="5"/>
          <RowDefinition Height="30"/>
          <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="100"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Button Style="{DynamicResource LargeRoundedButton}" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2">Choose Root Directory</Button>
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" ></TextBox>
        <Button Grid.Row="1" Grid.Column="1" Content="Open Folder Settings" />
        <Button Style="{DynamicResource LargeRoundedButton}" Grid.Row="3" Grid.Column="0" Grid.RowSpan="2" >Choose Presorted Directory</Button>
        <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" ></TextBox>
        <Button Grid.Row="4" Grid.Column="1" Content="Select Presorted Directory from Settings" />
      </Grid>
      <Grid Width="473">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="*" />
          <RowDefinition Height="*" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="100" />
          <ColumnDefinition Width="100" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button  Grid.Row="0" Grid.Column="0">Open Move Logs</Button>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch">
          <CheckBox Style="{DynamicResource StarCheckbox}" Width="20"/>
          <CheckBox Style="{DynamicResource StarCheckbox}" Width="20"/>
          <CheckBox Style="{DynamicResource StarCheckbox}" Width="20"/>
          <CheckBox Style="{DynamicResource StarCheckbox}" Width="20"/>
          <CheckBox Style="{DynamicResource StarCheckbox}" Width="20"/>
        </StackPanel>
        <Button Grid.Row="0" Grid.Column="2">Save Rating</Button>
        <Button Grid.Row="1" Grid.Column="0">File Convesion</Button>
        <Button Grid.Row="2" Grid.Column="0">View File Properties</Button>
        <Slider Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"></Slider>
        <Button Grid.Row="0" Grid.Column="3">Open Dupe Checker</Button>
      </Grid>

    </StackPanel>

  </Grid>
</UserControl>
